node_modules/
dist/
out/
.next/
.env
.env.production
.turbo/
*.log
*.tsbuildinfo
.superpowers/

# apps/agent writes real submission files here when run locally/manually
# tested — never demo/test output belonging to the repo.
exam-workspace/

# electron-builder output (apps/agent's dist:win) — built artifacts, not source.
release/

# Personal Claude Code project instructions — not shared with the team/repo.
CLAUDE.md


# Python — script calibration offline sinh bytecode
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Eval (spec 2026-09-20 §12.7) — lượt chạy ra file; lượt nào trích vào báo cáo thì commit có chủ đích (git add -f)
apps/api/eval/runs/*
!apps/api/eval/runs/.gitkeep
