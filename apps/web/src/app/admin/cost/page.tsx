import { Wallet } from 'lucide-react';
import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function CostPage() {
  return (
    <PlaceholderPage
      icon={Wallet}
      title="Chi phí"
      description="Theo dõi chi phí gọi AI theo giáo viên/khoa và ngân sách hàng tháng sẽ có ở đây."
    />
  );
}
