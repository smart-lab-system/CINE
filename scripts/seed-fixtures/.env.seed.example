# Copy to .env.seed.local (gitignored) and set a local password.
# Used by: pnpm seed:sample

API_BASE=http://localhost:4000

# Required. Same password is applied to bootstrap admin + every new account
# on first ensure. Do not commit the real value.
SEED_BOOTSTRAP_PASSWORD=
